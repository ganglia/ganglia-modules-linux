

Sample configure command:

export GANGLIA_HOME=/usr/src/ganglia-3.1.7

./configure \
  CFLAGS="`apr-1-config --cflags --includes` -I${GANGLIA_HOME}/include/ -I${GANGLIA_HOME}/lib/ -I${GANGLIA_HOME}/libmetrics/" \
  --enable-shared --disable-static

